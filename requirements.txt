# --- Coeur du RAG ---
mistralai>=1.5,<2          # embeddings (mistral-embed) + génération (chat)
qdrant-client>=1.12,<2     # base vectorielle

# --- Interface & configuration ---
streamlit>=1.40,<2         # interface utilisateur
python-dotenv>=1.0,<2      # chargement du fichier .env
